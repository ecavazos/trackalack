#search-box
  %form
    %input{ :type => 'text'}
    %input{ :type => 'submit', :value => 'Search'}

.row
  %div{ :class => 'column grid_6'}
    .home-button
      = link_to 'Add Client', new_client_path, :id => 'test'
  %div{ :class => 'column grid_6'}
    .home-button
      = link_to 'Add User', new_user_path

%h2
  Recent Activity

%ul.activity
  - @time_entries.each do |x|
    %li
      = link_to x.user.full_name, user_path(x.user)
      recently (#{x.created_at}) did work for
      = link_to x.project.client.name, client_path(x.project.client)
      on the
      = link_to x.project.name, project_path(x.project)
      project.
